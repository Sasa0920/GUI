<Page x:Class="Recipe_Management_App.FavouritesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Recipe_Management_App"
      mc:Ignorable="d" 
      Title="FavouritesPage">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel>
            <Grid Height="750">
                <Image Source="Images/background6.jpg" Stretch="Fill"/>
                <TextBlock 
                   FontSize="75"
                   FontWeight="Bold"
                   Foreground="Black"
                   VerticalAlignment="Center"
                   HorizontalAlignment="Center"
                   TextAlignment="Center"
                   Margin="0,100,0,40">
                   Add Your Favourite Recipes
    
                </TextBlock>

            </Grid>
            <ItemsControl ItemsSource="{Binding Favourites}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel HorizontalAlignment="Center"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="50" Width="230">
                            <Image Source="{Binding Image}" Height="250" Width="230" Stretch="UniformToFill"/>
                            <TextBlock Text="{Binding Name}" HorizontalAlignment="Center" FontSize="16" FontWeight="DemiBold" Margin="10"/>
                            <Border CornerRadius="5" BorderThickness="1" BorderBrush="White" Background="#FFABE4BC" Margin="65,10,65,0">
                                <Button Content="Delete" Padding="18,4" BorderThickness="0" FontSize="16" Background="Transparent" Foreground="Black"
                                        Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                        CommandParameter="{Binding Id}"/>

                            </Border>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
                
            </ItemsControl>
            
            <Grid Height="450" Background="#E2F7E2" >
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Top" Margin="80">
                    <TextBlock Text="Ready to start cooking?" FontSize="48" FontWeight="Bold" Foreground="Green" TextAlignment="Center"  />
                    <TextBlock Text="Collect, create, share recipes with TasteBook" FontSize="19" Margin="40" TextAlignment="Center" Foreground="Black" FontWeight="UltraLight"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="10"  HorizontalAlignment="Center">
                    <Image Source="Images/watsapp_icon.png" Height="20" Width="20" Margin="0,140,20,0"/>
                    <Image Source="Images/youtube_icon.png" Height="20" Width="20" Margin="0,140,20,0"/>
                    <Image Source="Images/instagram_icon.png" Height="20" Width="20" Margin="0,140,20,0"/>
                </StackPanel>
                <Rectangle Height="2" Width="1200" Fill="Gray" HorizontalAlignment="Stretch" VerticalAlignment="Bottom" Margin="80" />
                <TextBlock Text="Copyright © 2024 – All Rights Reserved"
                            FontSize="16"
                            FontWeight="DemiBold"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Bottom"
                            Margin="0,0,0,30"/>
            </Grid>
        </StackPanel>
        
    </ScrollViewer>
</Page>
